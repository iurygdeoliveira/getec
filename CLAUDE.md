# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Project Name:** Getec
**Status:** New project
**Description:** (To be filled in as project develops)

## Getting Started

This project is being initialized. As the project grows, this file will be updated with:

- **Tech Stack:** Document the primary languages, frameworks, and tools (e.g., Node.js, Python, Laravel, React, etc.)
- **Setup Instructions:** How to install dependencies and configure the development environment
- **Project Structure:** High-level organization of the codebase once files are added

## Common Commands

Once you establish the project structure, add commands here for:

- **Installing dependencies** (e.g., `npm install`, `composer install`, `pip install`)
- **Running in development** (e.g., `npm run dev`, `php artisan serve`)
- **Building for production** (e.g., `npm run build`, `cargo build`)
- **Running tests** (e.g., `npm test`, `pytest`, `cargo test`)
- **Linting and formatting** (e.g., `prettier`, `eslint`, `phpstan`)
- **Database migrations** (if applicable)

## Architecture

Document the high-level architecture once the project structure is in place:

- **Core modules/packages and their responsibilities**
- **Key design patterns or architectural decisions**
- **How data flows through the system**
- **Key dependencies and why they were chosen**

## Testing

Add testing guidelines when tests are introduced:

- **Test framework and configuration**
- **How to run tests (all tests, single test file, specific test)**
- **Testing conventions and patterns used in this project**

## Development Workflow

As the project evolves, document:

- **How to create a feature or fix a bug**
- **Pull request process and code review guidelines**
- **Git workflow (branch naming, commit message style)**
- **Any CI/CD pipelines or automated checks**

## Key Files and Directories

Document the purpose of major directories and key files once they exist:

- Root-level configuration files (tsconfig.json, tailwind.config.js, etc.)
- Source code organization (src/, app/, lib/, etc.)
- Build output locations
- Important scripts

## Troubleshooting

Add common issues and solutions as they arise during development.
